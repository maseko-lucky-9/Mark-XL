# Phase 1 Reflection (Planning / `specify`) — RARV

- **What worked:** Re-verifying the 4 bug file:line and the 17-action-file count against live code (B1 main.py:896, B3 executor.py:243-245, `ls actions/*.py` → 17) before writing made the spec defensible and let the verifier trace every claim to source — APPROVE on the first cycle (8/8 checks).
- **Friction / divergence:** The role boilerplate said "delegate plan authoring to `planner`", but the task said "emit the `specify` artifact." These conflict — `planner` produces the "How" (implementation plans), wrong altitude for a "What" spec, and a fresh planner-opus could re-derive the "18 tools" miscount. Resolved by authoring directly and preserving the author≠approver lane via the verifier gate (advisor-confirmed).
- **Key design tension caught:** B3's "unknown tool now raises" is a real behaviour change vs. baseline, which appears to conflict with the flag-OFF==baseline NFR. Resolved by classifying it explicitly as a bug *correction* (EC-5), not a flagged feature — consistent with "WO-0 gates nothing."
- **Carry forward to Analyze (Phase 2):** The 3 inline tools (`save_memory`, `agent_task`, `shutdown_jarvis`) are main-only and MUST stay out of any dual-path requirement — asserting them through both paths is a false requirement (EC-7). `shutdown_jarvis` calls `os._exit(0)` → must be mocked in every test that touches it.
- **Carry forward — count discipline:** The "18 tools" miscount is a recurring re-introduction risk (R-5). Downstream phases must treat §6 of spec.md (verbatim from intake.md:40-44) as the single source of truth for the tool list, not re-derive it.
