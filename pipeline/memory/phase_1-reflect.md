# Phase 1 Reflection (Planning — Spec Kit `specify`) — WO-6

- **What worked:** Research-first paid off immediately — reading the live `tests/test_flags.py` corrected the intake/reflection's mis-named `test_no_production_code_calls_get_flag`; the actual mechanism is `test_only_sanctioned_production_code_calls_get_flag` asserting an EXACT-SET `set(callers) == {"main.py", "agent/executor.py"}` (test_flags.py:139). FR-18 now treats extending that exact set (and only sanctioned files) as a first-class requirement.
- **What worked:** All verified facts (task_queue.py:9-47 in-memory list+lock, mark_xl_rust_adapter.py:17-25/56-62 WO-1 pattern, flags.json keys, missing pyproject/LICENSE/NOTICE) were pre-confirmed before delegating, so the planner produced a fully-cited spec with zero placeholders and the verifier APPROVED on cycle 1 (all 7 gates).
- **Friction:** The intake/reflection use slightly different names than the live code (the get_flag test name; "allowlist" vs exact-set). Carry-forward phases must trust the live tree over the prose. Also: `loopguard_*` keys in flags.json are int tunables, not booleans — WO-6's four new flags are all booleans, default false, never mix with tunables.
- **Carry-forward to Phase 2 (Analyze):** (1) The four WO-6 boolean flags are named and frozen — `use_scheduler_store`, `use_session_store`, `enable_trace_store`, `enable_telemetry`. (2) StoreExecutor MUST mirror the WO-1 adapter (no second threading mechanism) — Design phase should diagram main→worker(max_workers=1)→store→Qt-signal→UI. (3) Session persistence is DISTINCT from WO-4 memory_v2 fact memory (no shared storage). (4) Trace schema must be WO-7-discovery-ready (ordered tool-call sequences + outcomes/timestamps) but no engine code in WO-6.
- **Carry-forward to Phase 4 (Development):** keep decomposition to ~15-20 atomic tasks; if it exceeds 20, the non-blocking lane-split is A=scheduler+session(+migration) / B=trace+telemetry / C=packaging, with store_executor landing first as the shared dependency. Pin CI matrix (NFR-7/AC7) — local V&V is macOS-only, so Windows/Linux regressions only surface in CI.
